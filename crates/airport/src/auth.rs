// This file is intentionally unused.
// The `auth` module is defined via `src/auth/mod.rs`.
// In lib.rs, the module is declared with an explicit `#[path]` attribute
// to avoid the ambiguity error (E0761) that would occur if both files exist.
